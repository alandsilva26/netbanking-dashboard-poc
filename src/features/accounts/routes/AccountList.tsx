import { PageLayout } from 'components/Layout/PageLayout';
import { IAccount } from '../types';

export const AccountList = () => {
  const accountsDummyData: IAccount[] = [];

  return <PageLayout title="Customers"></PageLayout>;
};
